# NexaCare Environment Config
VITE_API_URL=          # Backend base URL
VITE_USE_MOCK=         # true = mock data | false = real API
VITE_WS_URL=           # WebSocket URL for real-time events (future)
VITE_WHATSAPP_BOT_URL= # WhatsApp bot webhook (future)
